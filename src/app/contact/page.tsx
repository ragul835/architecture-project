import type { Metadata } from 'next';
import { Suspense } from 'react';
import { Mail, Phone, Clock } from 'lucide-react';
import { InquiryForm } from '@/components/forms/InquiryForm';
import { STUDIO_CONFIG } from '@/data/studioConfig';

export const metadata: Metadata = { title: 'Book a Design Consultation', description: 'Share your location, property, scope, area, budget, and timing for an architecture, interior, or exterior design consultation.', alternates: { canonical: '/contact' } };
export default function ContactPage() { return <><header className="section-space border-b bg-[var(--surface)]"><div className="page-shell"><p className="eyebrow">Start a project</p><h1 className="display-title mt-5 max-w-4xl">A useful first conversation starts with a clear brief.</h1><p className="body-copy mt-6 max-w-3xl">Share what you know. Plot documents, photographs, and detailed drawings are not required in this first form.</p></div></header><section className="section-space"><div className="page-shell grid gap-8 lg:grid-cols-[1.4fr_.6fr]"><Suspense fallback={<div className="surface-card min-h-[500px] animate-pulse p-8">Loading inquiry form…</div>}><InquiryForm /></Suspense><aside className="space-y-6"><div className="surface-card p-6"><h2 className="font-display text-2xl">Direct contact</h2><ul className="mt-5 space-y-4 text-sm"><li><a href={`tel:${STUDIO_CONFIG.contact.phone.replace(/\s/g, '')}`} className="flex min-h-11 items-center gap-3"><Phone className="text-[var(--clay)]" size={18} />{STUDIO_CONFIG.contact.phone}</a></li><li><a href={`mailto:${STUDIO_CONFIG.contact.email}`} className="flex min-h-11 items-center gap-3 break-all"><Mail className="text-[var(--clay)]" size={18} />{STUDIO_CONFIG.contact.email}</a></li><li className="flex gap-3"><Clock className="shrink-0 text-[var(--clay)]" size={18} />{STUDIO_CONFIG.contact.hours}</li></ul></div><div className="surface-card p-6"><p className="text-xs font-bold uppercase tracking-wider text-[var(--clay)]">Service area note</p><p className="mt-3 text-sm leading-6 text-[var(--muted)]">{STUDIO_CONFIG.serviceArea.display}. Site visits and local consultant coordination are confirmed per enquiry.</p></div></aside></div></section></>; }
